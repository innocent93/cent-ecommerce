import React, { useEffect, useState } from 'react';
import { Ban, RotateCcw, Trash2 } from 'lucide-react';
import api from '../utils/api'; import { toast } from 'react-toastify';
export default function Customers(){ const [rows,setRows]=useState([]); const load=async()=>{try{const {data}=await api.get('/api/user/customers?includeDeleted=true');setRows(data.customers||[])}catch(e){toast.error(e.response?.data?.message||'Unable to load customers')}}; useEffect(()=>{load()},[]); const act=async(url,method='patch',data)=>{try{await api({url,method,data});toast.success('Updated');load()}catch(e){toast.error(e.response?.data?.message||'Action failed')}}; return <div className="space-y-6"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-blue-600">Customers</p><h1 className="mt-1 text-3xl font-extrabold">Customer management</h1><p className="mt-1 text-sm text-slate-500">Ban, restore or archive customer accounts without destroying order history.</p></div><div className="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="min-w-full text-sm"><thead className="bg-slate-50 text-left text-xs uppercase tracking-wider text-slate-500"><tr><th className="px-5 py-4">Customer</th><th className="px-5 py-4">State</th><th className="px-5 py-4 text-right">Actions</th></tr></thead><tbody className="divide-y divide-slate-100">{rows.map(r=><tr key={r.id}><td className="px-5 py-4"><div className="font-bold">{r.name}</div><div className="text-xs text-slate-500">{r.email}</div></td><td className="px-5 py-4">{r.banned?'Banned':r.deletedAt?'Archived':r.active?'Active':'Disabled'}</td><td className="px-5 py-4"><div className="flex justify-end gap-2">{r.deletedAt?<button onClick={()=>act(`/api/user/customers/${r.id}/restore`)} className="rounded-xl border px-3 py-2 text-xs font-bold"><RotateCcw size={14} className="mr-1 inline"/>Restore</button>:<><button onClick={()=>act(`/api/user/customers/${r.id}/ban`,'patch',{banned:!r.banned,reason:'Policy violation'})} className="rounded-xl border border-red-200 px-3 py-2 text-xs font-bold text-red-700"><Ban size={14} className="mr-1 inline"/>{r.banned?'Unban':'Ban'}</button><button onClick={()=>act(`/api/user/customers/${r.id}`,'delete')} className="rounded-xl border p-2 text-slate-500 hover:text-red-600"><Trash2 size={16}/></button></>}</div></td></tr>)}</tbody></table></div></div></div> }
